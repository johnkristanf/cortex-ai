OPENAI_API_KEY="sk-..."
FIRECRAWL_API_KEY="fc-e68e..."

LANGSMITH_API_KEY="lsv2_..."
LANGCHAIN_TRACING_V2="true"
LANGCHAIN_PROJECT="cortex-ai"

# Google Places API (New) – no server-side key needed.
# The user's Google OAuth access token (already in use for Gmail) is
# forwarded to places.googleapis.com on each request.
# Ensure the mobile OAuth flow requests the scope:
#   https://www.googleapis.com/auth/cloud-platform
# AND that the "Places API (New)" is enabled in your Google Cloud project.